<Window x:Class="ReorderingPhotos.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReorderingPhotos.UI" xmlns:usercontrols="clr-namespace:ReorderingPhotos.UI.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="376*"/>
            <RowDefinition Height="43*"/>
        </Grid.RowDefinitions>
        <usercontrols:PhotosList x:Name="photosList"   Grid.Row="0"  />

        <TextBox x:Name="photoSizeTextBox"  Text="{Binding PhotosSize}" Grid.Row="1"  />
    </Grid>
</Window>
